# CockroachDB Configuration
# Direct connection to specific node
DB_TYPE=cockroachdb
DB_HOST=localhost
DB_PORT=26257
DB_USERNAME=orders_user
DB_PASSWORD=
DB_NAME=orders_db

# For load-balanced connection (recommended for production)
# DB_HOST=localhost
# DB_PORT=26260

# Connection Pool Settings
DB_CONNECTION_LIMIT=20
DB_MAX_QUERY_EXECUTION_TIME=60000

# SSL Settings (set to true in production)
DB_SSL=false

# Legacy PostgreSQL Configuration (backup)
# DB_TYPE=postgres
# DB_HOST=127.0.0.1
# DB_PORT=5432
# DB_USERNAME=postgres
# DB_PASSWORD=password
# DB_NAME=orders_db
